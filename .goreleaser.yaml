version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: openapi-sdkgen
    main: ./cmd/openapi-sdkgen
    binary: openapi-sdkgen
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: default
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt
